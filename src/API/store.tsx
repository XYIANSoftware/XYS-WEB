export const storeAPI = () => {
    //*these will be axios calls for shopify api
    const getItems = () => {
        return;
    };
    const setItems = () => {};
    const purchaseCart = () => {
        return;
    };
    return { getItems, setItems, purchaseCart };
};
