// import { Module } from '@businessconnect/common-model';
// import { getRequest } from '@businessconnect/common-utils';

export const requests = () => {
    const requestNameCamelCase = () => {
        // return  getRequest(module, 'requestName(if any)', 'methodName', {params(if any)});
    };
    return { requestNameCamelCase };
};
