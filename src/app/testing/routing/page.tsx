'use client';
import { NextPage } from 'next';

interface Props {}

const Routing: NextPage<Props> = ({}) => {
    return (
        <div>
            <h5>nested</h5>
        </div>
    );
};

export default Routing;
