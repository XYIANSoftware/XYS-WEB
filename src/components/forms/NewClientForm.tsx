import React from 'react';
import { useForm } from 'react-hook-form';

const NewClientForm = () => {
    //TODO use hook form to create a basic new customer form and send to coda webhooks
    // const NewClientFrorm = useForm({});
    // const { control } = NewClientForm();

    return (
        <form id='xysnewclient'>
            <div>
                {/* name */}
                {/* email */}
                {/* phone */}
                {/* description of project request  */}
                {/* level 1-10 how clearly can you see your vision  */}
            </div>
        </form>
    );
};

export default NewClientForm;
